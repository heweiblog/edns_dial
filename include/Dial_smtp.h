#ifndef _DIAL_SMTP_H_
#define _DIAL_SMTP_H_

int handle_smtp_dialing(const char*ip,const int port,int* delay);
//int handle_smtp_dialing(const char*ip,const char* user,const char* pass,const int port,int* delay);

#endif
